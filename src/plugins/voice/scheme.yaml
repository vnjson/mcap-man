#
# Внутри плагина { hands } весит обработчик клика
# который запускает плагин { voicePlay }
# который в свою очередь беред данные из текущего
#   объекта контекста { ctx }, а именно из параметра { $voice }
#

- $voice: soundName # Не воспроизводится